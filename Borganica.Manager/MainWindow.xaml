<Window x:Class="Borganica.Manager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wpf="http://schemas.microsoft.com/netfx/2007/xaml/presentation"
        Title="MainWindow" Height="500" Width="800"
        >
    <Grid>
        <ListView Name="ProjectList">
            <ListView.View>
                <GridView AllowsColumnReorder="false">
                    <GridViewColumn Header="Id" Width="50" DisplayMemberBinding="{Binding Id}" />
                    <GridViewColumn Header="Name" Width="400" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="State" Width="350" DisplayMemberBinding="{Binding State}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
